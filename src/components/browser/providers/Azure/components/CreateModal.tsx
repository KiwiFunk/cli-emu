import type { CreateRepoFormProps } from "@/types";
export default function AzureCreateModal(props: CreateRepoFormProps) {
  return <div className="fixed inset-0 bg-black/50 flex items-center justify-center text-white z-50">Modal Stub</div>;
}
